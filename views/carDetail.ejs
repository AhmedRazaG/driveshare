<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Car Details - DriveShare</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
  <%- include('partials/header') %>
  <div class="container mt-4">
    <h2>Car Details</h2>
    <div class="card mb-4">
      <div class="card-body">
        <h5 class="card-title"><%= listing.model %> (<%= listing.year %>)</h5>
        <p class="card-text">
          Mileage: <%= listing.mileage %> miles<br>
          Location: <%= listing.pickupLocation %><br>
          Price: $<%= listing.rentalPrice %>/day<br>
          Availability: <%= listing.availability %>
        </p>
        <p class="text-danger"><small>You must verify your password before booking this car.</small></p>
        <form method="POST" action="/booking/verify/<%= listing.id %>">
          <div class="form-group">
            <label>Enter your Password</label>
            <input type="password" class="form-control" name="password" placeholder="Your password" required>
          </div>
          <button type="submit" class="btn btn-primary">Verify & Book</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
