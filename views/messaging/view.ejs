<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>View Message - DriveShare</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .message-header {
      background-color: #f8f9fa;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 20px;
    }
    .message-content {
      white-space: pre-wrap;
    }
    .message-meta {
      font-size: 0.9em;
      color: #6c757d;
    }
  </style>
</head>
<body class="bg-light">
  <%- include('../partials/header') %>

  <div class="container mt-4">
    <div class="row">
      <div class="col-md-8 offset-md-2">
        <div class="card">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Message</h4>
            <div>
              <a href="/messaging/inbox" class="btn btn-outline-secondary me-2">
                <i class="fas fa-arrow-left"></i> Back to Inbox
              </a>
              <form action="/messaging/delete/<%= message.id %>" method="POST" class="d-inline">
                <button type="submit" class="btn btn-outline-danger" 
                        onclick="return confirm('Are you sure you want to delete this message?')">
                  <i class="fas fa-trash"></i> Delete
                </button>
              </form>
            </div>
          </div>
          <div class="card-body">
            <div class="message-details mb-4">
              <div class="row mb-2">
                <div class="col-md-3"><strong>From:</strong></div>
                <div class="col-md-9"><%= message.senderEmail %></div>
              </div>
              <div class="row mb-2">
                <div class="col-md-3"><strong>To:</strong></div>
                <div class="col-md-9"><%= message.receiverEmail %></div>
              </div>
              <div class="row mb-2">
                <div class="col-md-3"><strong>Date:</strong></div>
                <div class="col-md-9"><%= new Date(message.createdAt).toLocaleString() %></div>
              </div>
            </div>
            
            <div class="message-content p-3 bg-light rounded">
              <%= message.message %>
            </div>
            
            <div class="mt-4">
              <a href="/messaging/compose?to=<%= message.senderEmail %>" class="btn btn-primary">
                <i class="fas fa-reply"></i> Reply
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    .message-content {
      white-space: pre-wrap;
      font-family: inherit;
    }
    
    .message-details {
      border-bottom: 1px solid #dee2e6;
      padding-bottom: 1rem;
    }
  </style>

  <%- include('../partials/footer') %>
</body>
</html> 