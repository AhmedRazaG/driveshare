<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>View Message - DriveShare</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .message-header {
      background-color: #f8f9fa;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 20px;
    }
    .message-content {
      white-space: pre-wrap;
    }
    .message-meta {
      font-size: 0.9em;
      color: #6c757d;
    }
  </style>
</head>
<body class="bg-light">
  <%- include('../partials/header') %>
  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-body">
            <div class="message-header">
              <h3 class="card-title"><%= message.subject || '(No Subject)' %></h3>
              <div class="message-meta">
                <p class="mb-1">
                  <strong>From:</strong> <%= message.senderName %> (<%= message.senderEmail %>)
                </p>
                <p class="mb-1">
                  <strong>To:</strong> <%= message.receiverName %> (<%= message.receiverEmail %>)
                </p>
                <p class="mb-0">
                  <strong>Date:</strong> <%= new Date(message.createdAt).toLocaleString() %>
                </p>
              </div>
            </div>

            <div class="message-content mb-4">
              <%= message.message %>
            </div>

            <div class="d-flex justify-content-between">
              <a href="/messaging" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Inbox
              </a>
              <a href="/messaging/compose?to=<%= message.senderId === req.session.user.id ? message.receiverEmail : message.senderEmail %>" class="btn btn-primary">
                <i class="fas fa-reply"></i> Reply
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- Font Awesome for icons -->
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html> 