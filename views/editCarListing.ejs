<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Edit Car Listing - DriveShare</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/dashboard">DriveShare</a>
  </nav>
  <div class="container mt-4">
    <h2 class="mb-4">Edit Listing: <%= listing.model %> (<%= listing.year %>)</h2>
    <form method="POST" action="/car/edit/<%= listing.id %>">
      <div class="form-group">
        <label>Availability:</label>
        <input type="text" class="form-control" name="availability" value="<%= listing.availability %>" required />
      </div>
      <div class="form-group">
        <label>Rental Price (per day):</label>
        <input type="number" class="form-control" name="rentalPrice" value="<%= listing.rentalPrice %>" required />
      </div>
      <button type="submit" class="btn btn-primary btn-block">Update Listing</button>
    </form>
  </div>
</body>
</html>
